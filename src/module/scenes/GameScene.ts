import { BaseScene } from "../../lib/scene.lib";

export class GameScene extends BaseScene {
  constructor() {
    super("GameScene");
  }
  public preload() {}

  public create() {}

  public update() {
    super.update();
  }
}
